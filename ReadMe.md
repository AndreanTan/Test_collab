# Perintah Git yang telah Dipelajari

`git add .`
=>mendaftarkan file baru ke dalam git (git tracked)
`git commit - m "message"`
=> mendafarkan file yang telah dirubah untuk, nantinya, dikirim ke git hosting